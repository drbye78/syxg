# XG Synthesizer - Optional Dependencies (Python 3.11+)
# ======================================================
# Install with: pip install -r requirements_optional.txt
# Or install specific features: pip install -r requirements_optional.txt --no-deps

# =============================================================================
# Real-Time Workstation (Vibexg)
# =============================================================================
# For physical MIDI port support
rtmidi>=2.1.0,<3.0.0          # Python 3.11 compatible

# For real-time audio output
sounddevice>=0.4.6,<0.5.0     # Python 3.11 compatible

# For enhanced TUI (Text User Interface)
rich>=13.5.0,<14.0.0          # Python 3.11 compatible

# =============================================================================
# Development & Testing
# =============================================================================
pytest>=7.3.0,<9.0.0
pytest-cov>=4.1.0,<5.0.0
pytest-asyncio>=0.21.0,<0.24.0
pytest-benchmark>=4.0.0,<5.0.0
pytest-xdist>=3.3.0,<4.0.0     # Parallel test execution

# Code quality
black>=23.0.0,<25.0.0
ruff>=0.1.0,<0.5.0
mypy>=1.5.0,<1.10.0
flake8>=6.1.0,<8.0.0
isort>=5.12.0,<6.0.0

# Type stubs for better type checking
types-PyYAML>=6.0.0
types-jsonschema>=4.17.0
types-numpy>=1.24.0

# =============================================================================
# Visualization
# =============================================================================
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<0.14.0

# =============================================================================
# Performance Profiling
# =============================================================================
py-spy>=0.3.14
memory-profiler>=0.61.0
line-profiler>=4.0.0

# =============================================================================
# Platform-Specific (Optional)
# =============================================================================
# Linux ALSA MIDI (alternative to rtmidi)
# python-alsa>=1.0.0,<2.0.0

# macOS CoreMIDI (alternative to rtmidi)  
# pyobjc-framework-CoreMIDI>=9.0.0,<10.0.0

# JACK audio connection
# jackclient>=0.3.0,<0.4.0

# Improved keyboard handling (for keyboard MIDI input)
# keyboard>=0.13.5,<0.14.0

# =============================================================================
# Installation Commands
# =============================================================================
# Core only:        pip install -r requirements.txt
# Workstation:      pip install -r requirements.txt -r requirements_optional.txt
# Development:      pip install -r requirements_dev.txt
# Full installation: pip install -e ".[full]"

