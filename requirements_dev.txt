# XG Synthesizer - Development Dependencies (Python 3.14+)
# =========================================================
# Install with: pip install -r requirements_dev.txt
# Or use: pip install -e ".[dev]"

# =============================================================================
# Type Checking
# =============================================================================
mypy>=1.11.0,<1.15.0
types-PyYAML>=6.0.12.20250915
types-jsonschema>=4.23.0.20260402
#types-numpy>=1.26.0

# =============================================================================
# Linting & Code Quality
# =============================================================================
ruff>=0.6.0,<0.10.0
flake8>=7.0.0,<8.0.0
black>=24.0.0,<25.0.0
isort>=5.13.0,<6.0.0

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0
pytest-asyncio>=0.24.0,<0.25.0
pytest-benchmark>=4.0.0,<5.0.0
pytest-xdist>=3.6.0,<4.0.0     # Parallel test execution

# =============================================================================
# Performance Profiling
# =============================================================================
py-spy>=0.4.1                # Sampling profiler
memory-profiler>=0.61.0       # Memory usage profiling
line-profiler>=5.0.2          # Line-by-line profiling

# =============================================================================
# Documentation
# =============================================================================
sphinx>=7.0.0,<9.0.0
sphinx-rtd-theme>=2.0.0
sphinx-autobuild>=2024.0.0
myst-parser>=3.0.0

# =============================================================================
# Pre-commit Hooks
# =============================================================================
pre-commit>=3.5.0

# =============================================================================
# Build & Release
# =============================================================================
build>=1.0.0,<2.0.0
twine>=5.0.0,<7.0.0
wheel>=0.44.0,<0.46.0

# =============================================================================
# Installation
# =============================================================================
# Install all development dependencies:
#   pip install -r requirements_dev.txt
#
# Or install from pyproject.toml:
#   pip install -e ".[dev]"
#
# Install pre-commit hooks:
#   pre-commit install
#
# Run all checks:
#   pre-commit run --all-files
#
# Run tests:
#   pytest tests/ -v
#
# Run type checking:
#   mypy synth/ vibexg/
#
# Run linting:
#   ruff check synth/ vibexg/
#   black --check synth/ vibexg/
